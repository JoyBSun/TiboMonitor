# TiboMonitor v1.1.2

本版本增加监控总开关和普通原创帖子提醒开关。

## 新功能

- “监控”区域新增“启用监控”，默认开启。
- 关闭监控后停止定时和手动网络检查，但保留托盘、已有未读和本地数据。
- 重新开启监控后立即检查一次，并恢复设定周期。
- “提醒内容”新增 Original 普通原创帖子开关，默认开启。
- 可分别开关 Original、Reply、Quote 和 Repost 提醒。
- 关闭某类提醒时仍将帖子静默记录为已见，重新开启后不会补弹旧内容。
- 设置窗口继续支持屏幕自适应、底部固定按钮和 5～1440 分钟间隔。
- 托盘菜单提供“设置...”。
- 可切换提醒窗口始终置顶。
- 可切换 Windows 登录后自动启动。
- 可从设置窗口打开数据目录和日志目录。
- 设置保存后立即生效，检查计时器自动重启。

## 质量

- 配置使用临时文件后原子替换。
- 自动测试增加到 14 项。
- Windows x64 自包含发布，无需单独安装 .NET。

## 安装

下载 `TiboMonitor-win-x64-v1.1.2.zip`，完整解压后双击 `Install.cmd`。

## 已知限制

- 默认数据来自第三方公开镜像，可能限流、停机或改变页面结构。
- 当前版本未进行商业代码签名，Windows SmartScreen 可能显示未知发布者。
